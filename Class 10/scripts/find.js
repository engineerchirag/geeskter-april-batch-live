/*
    findIndex is used for finding whether element exists in array
    It returns -1 if not element is found
    It returnd the index if element is found
*/

let array = [1, 2, 3, 5];

let result = array.findIndex((x) => x === 3)

console.log(result)